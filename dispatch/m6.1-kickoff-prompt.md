# Claude Code kickoff — M6.1 HEIC + Photo Cache GC

You are implementing the **M6.1** follow-up slice for ClutterCatcher (CCv4),
locally in this repo. Branch `feat/m6.1-heic-gc` off `main`.

Read first, in order:
1. `planning/ccv4-plan.md` — locked decisions D1–D17.
2. `OPEN_ITEMS.md` — decision log through DL50; note the resolved Run 4
   "Questions for Owen" (items 1 and 3 are this slice's mandate) and the
   DL43/DL44 photo-lifecycle decisions this builds on.
3. `planning/m6-photos-plan.md` — the M6 parent slice (P1–P14).
4. `planning/m6.1-heic-gc-plan.md` — **authoritative for this run**:
   decisions P15–P20, design notes, tests (§4), VERIFY (§5), touch-list (§6).

Ground rules:
- XcodeGen is source of truth (D5): `xcodegen generate` after any file adds;
  never hand-edit the `.xcodeproj`.
- Swift 6 strict concurrency; iOS 26 API surface only (D3).
- This slice is **local-only**: no schema change, no migration, no record
  fields, no CloudKit deploy, and the two write paths (DL20) must not be
  touched — the GC's database access is read-only.
- The file-naming contract is P16-frozen: `Photos/<ref>.jpg` /
  `<ref>_thumb.jpg` regardless of encoding. Do not rename, migrate, or add
  extension-sniffing lookups.
- The sweep's live set MUST include orphan-buffered item refs (P18) and MUST
  honor the 1-hour age guard (P19). These are correctness constraints, not
  polish.
- If a local Xcode toolchain is available, run `scripts/test.sh` and deliver
  it green. If you are in an environment without one, say so plainly (Run 1/4
  precedent) — never fake or stub a verification.
- Log implementation decisions to `OPEN_ITEMS.md` starting at **DL51**
  (reserved for the HEIC switch). Anything the plan doesn't answer →
  "Questions for Owen" with the most-reversible interim answer; don't guess
  silently.

Deliver: implement the §6 touch-list per P15–P20, add the §4 tests, then
output the §5 VERIFY checklist — agent-verifiable evidence where possible,
and the on-device items listed for Owen. Stop at the gate.
